### --- DEVELOPER NOTE --- ###
# This template is to be used with:
# - functions that check specific ADaM datasets, e.g. `check_adam`
# - functions that check ADaM requirements, e.g. `check_has_one_record_per_subject`
### --- END DEVELOPER NOTE --- ###

#' @return
#' Checker function that takes an ADaM dataset as argument and returns a `logical` check result.
#' See `?check_adam` for example.

