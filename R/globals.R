# these variables are defined to
# avoid R CMD check "no visible binding for global variable" notes
globalVariables(c("PARAMCD",
                  "USUBJID",
                  "records",
                  "subjects"))
